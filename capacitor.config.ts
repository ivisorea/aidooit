import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'aidooit_ionic_react_app',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
